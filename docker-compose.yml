name: ai-sre-platform

x-app-environment: &app-environment
  ENVIRONMENT: ${ENVIRONMENT:-development}
  LOG_LEVEL: ${LOG_LEVEL:-INFO}
  SERVICE_VERSION: ${SERVICE_VERSION:-0.1.0}
  TELEMETRY_ENABLED: ${TELEMETRY_ENABLED:-true}
  OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4317}
  OTEL_EXPORT_TIMEOUT_SECONDS: ${OTEL_EXPORT_TIMEOUT_SECONDS:-2.0}
  OTEL_BATCH_SCHEDULE_DELAY_MILLISECONDS: ${OTEL_BATCH_SCHEDULE_DELAY_MILLISECONDS:-500}
  OUTBOUND_HTTP_TIMEOUT_SECONDS: ${OUTBOUND_HTTP_TIMEOUT_SECONDS:-5.0}
  OUTBOUND_HTTP_MAX_ATTEMPTS: ${OUTBOUND_HTTP_MAX_ATTEMPTS:-2}
  OUTBOUND_HTTP_RETRY_BACKOFF_SECONDS: ${OUTBOUND_HTTP_RETRY_BACKOFF_SECONDS:-0.05}

x-database-environment: &database-environment
  POSTGRES_HOST: postgres
  POSTGRES_PORT: "5432"
  POSTGRES_DB: ${POSTGRES_DB:-aisre}
  POSTGRES_USER: ${POSTGRES_USER:-aisre}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
  DATABASE_CONNECT_TIMEOUT_SECONDS: ${DATABASE_CONNECT_TIMEOUT_SECONDS:-3.0}

x-queue-environment: &queue-environment
  CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
  CELERY_RESULT_BACKEND_URL: ${CELERY_RESULT_BACKEND_URL:-redis://redis:6379/1}
  QUEUE_PUBLISH_TIMEOUT_SECONDS: ${QUEUE_PUBLISH_TIMEOUT_SECONDS:-2.0}
  INVESTIGATION_MAX_ATTEMPTS: ${INVESTIGATION_MAX_ATTEMPTS:-3}
  INVESTIGATION_RETRY_BASE_SECONDS: ${INVESTIGATION_RETRY_BASE_SECONDS:-2}
  INVESTIGATION_RETRY_MAX_SECONDS: ${INVESTIGATION_RETRY_MAX_SECONDS:-30}
  INVESTIGATION_JOB_LEASE_SECONDS: ${INVESTIGATION_JOB_LEASE_SECONDS:-15}
  CELERY_VISIBILITY_TIMEOUT_SECONDS: ${CELERY_VISIBILITY_TIMEOUT_SECONDS:-300}

x-evidence-environment: &evidence-environment
  PROMETHEUS_URL: http://prometheus:9090
  LOKI_URL: http://loki:3100
  TEMPO_URL: http://tempo:3200
  EVIDENCE_HTTP_TIMEOUT_SECONDS: ${EVIDENCE_HTTP_TIMEOUT_SECONDS:-2.0}
  EVIDENCE_HTTP_MAX_ATTEMPTS: ${EVIDENCE_HTTP_MAX_ATTEMPTS:-2}
  EVIDENCE_HTTP_RETRY_BACKOFF_SECONDS: ${EVIDENCE_HTTP_RETRY_BACKOFF_SECONDS:-0.05}
  EVIDENCE_SOURCE_TIMEOUT_SECONDS: ${EVIDENCE_SOURCE_TIMEOUT_SECONDS:-8.0}
  EVIDENCE_MAX_RESPONSE_BYTES: ${EVIDENCE_MAX_RESPONSE_BYTES:-2097152}
  EVIDENCE_MAX_WINDOW_SECONDS: ${EVIDENCE_MAX_WINDOW_SECONDS:-3600}
  EVIDENCE_MAX_LOOKBACK_SECONDS: ${EVIDENCE_MAX_LOOKBACK_SECONDS:-21600}
  EVIDENCE_FUTURE_SKEW_SECONDS: ${EVIDENCE_FUTURE_SKEW_SECONDS:-300}
  EVIDENCE_DEPLOYMENT_LOOKBACK_SECONDS: ${EVIDENCE_DEPLOYMENT_LOOKBACK_SECONDS:-7200}
  EVIDENCE_METRIC_STEP_SECONDS: ${EVIDENCE_METRIC_STEP_SECONDS:-5}
  EVIDENCE_LOG_LIMIT: ${EVIDENCE_LOG_LIMIT:-50}
  EVIDENCE_TRACE_LIMIT: ${EVIDENCE_TRACE_LIMIT:-10}
  EVIDENCE_DEPLOYMENT_LIMIT: ${EVIDENCE_DEPLOYMENT_LIMIT:-10}
  EVIDENCE_CORRELATION_LIMIT: ${EVIDENCE_CORRELATION_LIMIT:-1000}
  EVIDENCE_SLOW_TRACE_THRESHOLD_MS: ${EVIDENCE_SLOW_TRACE_THRESHOLD_MS:-500}

x-investigator-environment: &investigator-environment
  INVESTIGATOR_ENABLED: ${INVESTIGATOR_ENABLED:-false}
  INVESTIGATOR_PROVIDER: ${INVESTIGATOR_PROVIDER:-openai}
  INVESTIGATOR_PLANNING_MODEL: ${INVESTIGATOR_PLANNING_MODEL:-gpt-5-mini}
  INVESTIGATOR_REASONING_MODEL: ${INVESTIGATOR_REASONING_MODEL:-gpt-5-mini}
  OPENAI_API_KEY: ${OPENAI_API_KEY:-}
  INVESTIGATOR_MODEL_TIMEOUT_SECONDS: ${INVESTIGATOR_MODEL_TIMEOUT_SECONDS:-30.0}
  INVESTIGATOR_MODEL_MAX_ATTEMPTS: ${INVESTIGATOR_MODEL_MAX_ATTEMPTS:-2}
  INVESTIGATOR_MAX_OUTPUT_TOKENS_PER_CALL: ${INVESTIGATOR_MAX_OUTPUT_TOKENS_PER_CALL:-2048}
  INVESTIGATOR_MAX_MODEL_CALLS: ${INVESTIGATOR_MAX_MODEL_CALLS:-16}
  INVESTIGATOR_MAX_TOOL_CALLS: ${INVESTIGATOR_MAX_TOOL_CALLS:-16}
  INVESTIGATOR_MAX_ITERATIONS: ${INVESTIGATOR_MAX_ITERATIONS:-2}
  INVESTIGATOR_MAX_CONTEXT_CHARS: ${INVESTIGATOR_MAX_CONTEXT_CHARS:-30000}
  INVESTIGATOR_MAX_TOTAL_TOKENS: ${INVESTIGATOR_MAX_TOTAL_TOKENS:-50000}
  INVESTIGATOR_MAX_DURATION_SECONDS: ${INVESTIGATOR_MAX_DURATION_SECONDS:-120.0}
  INVESTIGATOR_MAX_ESTIMATED_COST_USD: ${INVESTIGATOR_MAX_ESTIMATED_COST_USD:-2.0}
  INVESTIGATOR_INPUT_COST_PER_MILLION_USD: ${INVESTIGATOR_INPUT_COST_PER_MILLION_USD:-0.0}
  INVESTIGATOR_OUTPUT_COST_PER_MILLION_USD: ${INVESTIGATOR_OUTPUT_COST_PER_MILLION_USD:-0.0}
  INVESTIGATOR_ROOT_CONFIDENCE_THRESHOLD: ${INVESTIGATOR_ROOT_CONFIDENCE_THRESHOLD:-0.65}
  INVESTIGATOR_MIN_COMPETING_HYPOTHESES: ${INVESTIGATOR_MIN_COMPETING_HYPOTHESES:-3}
  KNOWLEDGE_PROVIDER: ${KNOWLEDGE_PROVIDER:-fake}
  KNOWLEDGE_EMBEDDING_MODEL: ${KNOWLEDGE_EMBEDDING_MODEL:-text-embedding-3-small}
  KNOWLEDGE_EMBEDDING_DIMENSIONS: ${KNOWLEDGE_EMBEDDING_DIMENSIONS:-1536}
  KNOWLEDGE_CHUNK_TOKENS: ${KNOWLEDGE_CHUNK_TOKENS:-600}
  KNOWLEDGE_CHUNK_OVERLAP_TOKENS: ${KNOWLEDGE_CHUNK_OVERLAP_TOKENS:-100}
  KNOWLEDGE_TOP_K: ${KNOWLEDGE_TOP_K:-8}
  KNOWLEDGE_MAX_TOP_K: ${KNOWLEDGE_MAX_TOP_K:-20}
  KNOWLEDGE_MAX_CONTEXT_CHARS: ${KNOWLEDGE_MAX_CONTEXT_CHARS:-6000}
  KNOWLEDGE_MAX_CHUNK_CHARS: ${KNOWLEDGE_MAX_CHUNK_CHARS:-2000}
  KNOWLEDGE_EMBEDDING_TIMEOUT_SECONDS: ${KNOWLEDGE_EMBEDDING_TIMEOUT_SECONDS:-10.0}

x-demo-service: &demo-service
  image: ai-sre-platform:stage05
  build:
    context: .
    dockerfile: infrastructure/docker/demo.Dockerfile
  init: true
  read_only: true
  tmpfs:
    - /tmp:size=16m,mode=1777
  security_opt:
    - no-new-privileges:true
  cap_drop:
    - ALL

services:
  postgres:
    image: pgvector/pgvector:0.8.0-pg17
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-aisre}
      POSTGRES_USER: ${POSTGRES_USER:-aisre}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./infrastructure/docker/postgres/init-test-db.sql:/docker-entrypoint-initdb.d/10-test-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 2s
      timeout: 3s
      retries: 20
      start_period: 5s

  redis:
    image: redis:8.2.9-alpine
    command: ["redis-server", "--appendonly", "yes", "--save", "60", "1"]
    ports:
      - "127.0.0.1:${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 3s
      retries: 20
      start_period: 3s

  migrate:
    <<: *demo-service
    command: ["alembic", "upgrade", "head"]
    environment:
      <<: [*app-environment, *database-environment]
    depends_on:
      postgres:
        condition: service_healthy

  tempo:
    image: grafana/tempo:2.10.7
    command: ["-config.file=/etc/tempo/tempo.yaml"]
    volumes:
      - ./observability/tempo/tempo.yaml:/etc/tempo/tempo.yaml:ro
      - tempo-data:/var/tempo
    ports:
      - "127.0.0.1:${TEMPO_PORT:-3200}:3200"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "/tempo", "-version"]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s

  loki:
    image: grafana/loki:3.7.4
    command: ["-config.file=/etc/loki/loki.yaml"]
    volumes:
      - ./observability/loki/loki.yaml:/etc/loki/loki.yaml:ro
      - loki-data:/loki
    ports:
      - "127.0.0.1:${LOKI_PORT:-3100}:3100"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "/usr/bin/loki", "-version"]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.153.0
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    read_only: true
    volumes:
      - ./observability/otel_collector/config.yaml:/etc/otelcol-contrib/config.yaml:ro
    ports:
      - "127.0.0.1:${OTEL_GRPC_PORT:-4317}:4317"
      - "127.0.0.1:${OTEL_HTTP_PORT:-4318}:4318"
      - "127.0.0.1:${OTEL_HEALTH_PORT:-13133}:13133"
    depends_on:
      tempo:
        condition: service_healthy
      loki:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test:
        [
          "CMD",
          "/otelcol-contrib",
          "validate",
          "--config=/etc/otelcol-contrib/config.yaml",
        ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  inventory-service:
    <<: *demo-service
    command:
      - uvicorn
      - apps.demo.inventory_service.main:create_app
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --no-access-log
    environment:
      <<: *app-environment
      INVENTORY_SKU: ${INVENTORY_SKU:-widget-001}
      INVENTORY_STOCK: ${INVENTORY_STOCK:-100}
      INVENTORY_UNIT_PRICE_CENTS: ${INVENTORY_UNIT_PRICE_CENTS:-1999}
    ports:
      - "127.0.0.1:${INVENTORY_SERVICE_PORT:-8003}:8000"
    healthcheck: &application-healthcheck
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/ready', timeout=2)",
        ]
      interval: 3s
      timeout: 3s
      retries: 15
      start_period: 5s

  payment-service:
    <<: *demo-service
    command:
      - uvicorn
      - apps.demo.payment_service.main:create_app
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --no-access-log
    environment:
      <<: [*app-environment, *database-environment]
      FAULT_INJECTION_ALLOWED: "true"
      FAULT_CONTROL_TOKEN: ${FAULT_CONTROL_TOKEN:-local-demo-fault-control}
      SLOW_DATABASE_DELAY_SECONDS: ${SLOW_DATABASE_DELAY_SECONDS:-2.5}
    ports:
      - "127.0.0.1:${PAYMENT_SERVICE_PORT:-8004}:8000"
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck: *application-healthcheck

  alert-receiver:
    <<: *demo-service
    profiles: ["test-tools"]
    command:
      - uvicorn
      - apps.demo.alert_receiver.main:create_app
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --no-access-log
    environment:
      <<: *app-environment
    ports:
      - "127.0.0.1:${ALERT_RECEIVER_PORT:-8005}:8000"
    healthcheck: *application-healthcheck

  incident-api:
    <<: *demo-service
    command:
      - uvicorn
      - apps.incident_api.main:create_app
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --no-access-log
    environment:
      <<: [*app-environment, *database-environment, *queue-environment]
    ports:
      - "127.0.0.1:${INCIDENT_API_PORT:-8006}:8000"
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck: *application-healthcheck

  investigator-worker:
    <<: *demo-service
    hostname: investigator-worker
    command:
      - celery
      - -A
      - apps.investigator_worker.celery_app:celery_app
      - worker
      - --loglevel=INFO
      - --concurrency=1
      - --hostname=investigator@%h
    environment:
      <<: [
        *app-environment,
        *database-environment,
        *queue-environment,
        *evidence-environment,
        *investigator-environment,
      ]
    ports:
      - "127.0.0.1:${INVESTIGATOR_METRICS_PORT:-9464}:9464"
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      prometheus:
        condition: service_healthy
      loki:
        condition: service_healthy
      tempo:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "celery",
          "-A",
          "apps.investigator_worker.celery_app:celery_app",
          "inspect",
          "ping",
          "--destination",
          "investigator@investigator-worker",
          "--timeout",
          "2",
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  alertmanager:
    image: prom/alertmanager:v0.33.1
    command:
      - --config.file=/etc/alertmanager/alertmanager.yml
      - --storage.path=/alertmanager
    volumes:
      - ./observability/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager-data:/alertmanager
    ports:
      - "127.0.0.1:${ALERTMANAGER_PORT:-9093}:9093"
    depends_on:
      incident-api:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test:
        [
          "CMD",
          "/bin/amtool",
          "check-config",
          "/etc/alertmanager/alertmanager.yml",
        ]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s

  order-service:
    <<: *demo-service
    command:
      - uvicorn
      - apps.demo.order_service.main:create_app
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --no-access-log
    environment:
      <<: *app-environment
      INVENTORY_SERVICE_URL: http://inventory-service:8000
      PAYMENT_SERVICE_URL: http://payment-service:8000
    ports:
      - "127.0.0.1:${ORDER_SERVICE_PORT:-8002}:8000"
    depends_on:
      inventory-service:
        condition: service_healthy
      payment-service:
        condition: service_healthy
    healthcheck: *application-healthcheck

  gateway:
    <<: *demo-service
    command:
      - uvicorn
      - apps.demo.gateway.main:create_app
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --no-access-log
    environment:
      <<: *app-environment
      ORDER_SERVICE_URL: http://order-service:8000
    ports:
      - "127.0.0.1:${GATEWAY_PORT:-8001}:8000"
    depends_on:
      order-service:
        condition: service_healthy
    healthcheck: *application-healthcheck

  prometheus:
    image: prom/prometheus:v3.13.0
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=6h
    volumes:
      - ./observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./observability/prometheus/rules:/etc/prometheus/rules:ro
      - prometheus-data:/prometheus
    ports:
      - "127.0.0.1:${PROMETHEUS_PORT:-9090}:9090"
    depends_on:
      gateway:
        condition: service_started
      order-service:
        condition: service_started
      inventory-service:
        condition: service_started
      payment-service:
        condition: service_started
      incident-api:
        condition: service_started
      alertmanager:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "/bin/promtool", "check", "ready", "--url=http://127.0.0.1:9090"]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s

  frontend:
    build:
      context: .
      dockerfile: infrastructure/docker/frontend.Dockerfile
      args:
        VITE_APPROVAL_ACTOR: ${VITE_APPROVAL_ACTOR:-local-demo-approver}
    ports:
      - "127.0.0.1:${FRONTEND_PORT:-5173}:80"
    depends_on:
      incident-api:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    # nginx master starts as root (bind :80, cache setup) then drops to the
    # nginx user, so it keeps only the capabilities it needs instead of
    # the blanket cap_drop: ALL used by the Python services.
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - NET_BIND_SERVICE
      - SETGID
      - SETUID
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:80/health/live >/dev/null"]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 10s

  grafana:
    image: grafana/grafana:13.2.0
    environment:
      GF_ANALYTICS_REPORTING_ENABLED: "false"
      GF_ANALYTICS_CHECK_FOR_UPDATES: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
      GF_AUTH_DISABLE_LOGIN_FORM: "true"
    volumes:
      - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    ports:
      - "127.0.0.1:${GRAFANA_PORT:-3000}:3000"
    depends_on:
      prometheus:
        condition: service_healthy
      loki:
        condition: service_healthy
      tempo:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:3000/api/health >/dev/null"]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 10s

volumes:
  postgres-data:
  prometheus-data:
  loki-data:
  tempo-data:
  grafana-data:
  alertmanager-data:
  redis-data:
